{
  "name": "mudchat",
  "version": "1.0.0",
  "description": "",
  "main": "test/server.js",
  "scripts": {
    "start": "node ./test/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node --debug ./test/server.js",
    "dev": "node --debug ./test/server.js & PIDDBG=$! && open http://localhost:8080 && node-inspector --save-live-edit || kill -9 $PIDDBG",
    "debug-start": "node --debug-brk ./test/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.10.6",
    "printf": "^0.2.3"
  },
  "devDependencies": {
    "code": "^2.0.1",
    "lab": "^7.2.0"
  }
}
